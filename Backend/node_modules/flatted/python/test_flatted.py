import unittest
from flatted import _Known

class TestKnown(unittest.TestCase):
    def test_init(self):
        known = _Known()
        self.assertEqual(known.key, [])
        self.assertEqual(known.value, [])

if __name__ == '__main__':
    unittest.main()